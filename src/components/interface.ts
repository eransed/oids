export interface Button90Config {
  buttonText: string;
  clickCallback: () => void;
  selected: boolean;
}

export interface User {
  createdAt: string;
  email: string;
  id: string;
  updatedAt: string;
}
