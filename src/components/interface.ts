export interface Button90Config {
  buttonText: string
  clickCallback: () => void
  selected: boolean
}

export interface User {
  id: string
  email: string
  name: string
  createdAt: string
  updatedAt: string
}
